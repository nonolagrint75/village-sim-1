import fs from "fs"
const t = fs.readFileSync("src/lib/render/nature/draw.ts", "utf8")
const j = t.indexOf("export function naturePixel32")
const k = t.indexOf("export function natureHandlesTerrain")
console.log(t.slice(j, k))
console.log("---shoreField in pixel---")
console.log(/shoreField/.test(t.slice(j,k)), /shoreFieldFast/.test(t.slice(j,k)))
console.log("---shorePaint shoreField---")
const s = fs.readFileSync("src/lib/render/nature/shorePaint.ts", "utf8")
const a = s.indexOf("export function shoreField")
const b = s.indexOf("export function shoreFieldFast")
console.log(s.slice(a, b))
const c = s.indexOf("const f = shoreField(terrain")
console.log("---strip sample---")
console.log(s.slice(c, c+220))