/**
 * Optional WASM placeholder.
 * Replaced by: cd sim-core && wasm-pack build --target web --out-dir ../src/lib/sim/kernels/pkg
 * Until then the app uses TypeScript kernels (SIM_CORE_STUB stays true).
 */
export const SIM_CORE_STUB = true
